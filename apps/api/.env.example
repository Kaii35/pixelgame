NODE_ENV=development
API_PORT=3001
API_HOST=0.0.0.0
API_CORS_ORIGIN=http://localhost:5173

# Data layer: `memory` (default, no Docker) or `postgres`.
# DATABASE_URL / REDIS_URL are only consulted when PIXEL_DB=postgres.
PIXEL_DB=memory
DATABASE_URL=postgresql://pixelgame:pixelgame_dev_password@localhost:5432/pixelgame?schema=public
REDIS_URL=redis://localhost:6379

JWT_PRIVATE_KEY_PATH=../../keys/jwt-private.pem
JWT_PUBLIC_KEY_PATH=../../keys/jwt-public.pem
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
JWT_ISSUER=pixelgame
JWT_AUDIENCE=pixelgame-client
